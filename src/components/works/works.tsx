import Section from '../section/section';
import { StyledWorks } from './works.style';

function Works() {
  return (
    <StyledWorks>
      <Section heading='WORKS' subheading={`What've I done so far?`}>
        s
      </Section>
    </StyledWorks>
  );
}

export default Works;
